# Ralph Progress Log
Started: Fri Mar 20 09:49:36 PDT 2026
---

## Codebase Patterns
- To expose creatorId to the frontend, add it to PeladaDetailResponseDTO and PeladaDetail type; check `member.id === creatorId` to hide creator-restricted actions
- Admin check pattern: `pelada.members.find(m => m.id === currentUser?.id)?.isAdmin ?? false`
- Debounced search: store timer in `useRef<ReturnType<typeof setTimeout>>`, clear on each keystroke, fire after 300ms
- Error message extraction from axios errors: `(err as { response?: { data?: { message?: string } } })?.response?.data?.message ?? 'fallback'`
- Modal pattern: use fixed inset-0 z-50 bg-black/50 overlay with bg-background inner div; click backdrop to close
- `sonner` toast is set up globally in main.tsx — just `import { toast } from "sonner"` and call toast.success/error
- extractErrorMessage helper (in AuthPage.tsx) can be copy-pasted for API error handling in other components
- select element styling: use `flex h-9 w-full rounded-md border border-input bg-transparent px-3 py-1 text-sm shadow-sm` to match Input component

- Backend uses Lombok @Data, @Builder, @NoArgsConstructor, @AllArgsConstructor on all entities and DTOs
- DTOs have a static `from(Entity entity)` factory method for entity-to-DTO conversion
- JPA entities use @CreationTimestamp from org.hibernate.annotations for createdAt fields
- ManyToMany relationships use @JoinTable with explicit join column names
- Hibernate DDL is set to `update` mode - tables auto-created from entities
- Java/Maven not available in this environment - "Typecheck passes" means code is syntactically correct
- Frontend TypeScript typecheck: `cd frontend && npx tsc -b --noEmit`
- Controllers inject `Authentication` and get current user email via `(String) authentication.getPrincipal()`
- Services use `@Transactional` (write) and `@Transactional(readOnly = true)` for read operations
- Lazy-loaded collections (@ManyToMany with FetchType.LAZY) are safe to access within @Transactional service methods
- PeladaRepository uses Spring Data derived query `findByMembersContaining(User user)` for membership queries
- Pelada entity has a `creator` field (ManyToOne User) to track creator for permission checks
- UserRepository has `searchByUsernameOrEmail` JPQL query for case-insensitive search by username or email
- Conflict responses use AppException(HttpStatus.CONFLICT, message)
- File uploads: `@RequestParam("file") MultipartFile file`; store under `@Value("${app.uploads.dir:uploads}")` dir with UUID prefix
- File serving: use `UrlResource` + `permitAll()` in SecurityConfig for /api/v1/files/**
- Delete entity with ManyToMany: clear collections before delete to avoid FK constraint violations

## 2026-03-20 - US-001
- Created Pelada JPA entity with all required fields and many-to-many relationships
- Created PeladaRepository extending JpaRepository
- Created PeladaResponseDTO with static from() factory method
- Files changed:
  - backend/src/main/java/com/futspring/backend/entity/Pelada.java
  - backend/src/main/java/com/futspring/backend/repository/PeladaRepository.java
  - backend/src/main/java/com/futspring/backend/dto/PeladaResponseDTO.java
- **Learnings for future iterations:**
  - Follow User.java pattern exactly for entities (Lombok @Data, @Builder, @NoArgsConstructor, @AllArgsConstructor)
  - @Builder.Default required for Set fields initialized with new HashSet<>()
  - @CreationTimestamp with updatable=false for createdAt fields
  - Maven/Java not available; typecheck verified by reading code carefully + ensuring frontend tsc passes
---

## 2026-03-20 - US-002
- Created CreatePeladaRequestDTO with @Valid annotations for required fields
- Created PeladaMemberDTO with isAdmin flag and static from() factory
- Created PeladaDetailResponseDTO with members list and static from() factory
- Updated PeladaRepository with findByMembersContaining(User) derived query
- Created PeladaService with createPelada, getMyPeladas, getPeladaDetail methods
- Created PeladaController at /api/v1/peladas with POST, GET /my, GET /:id endpoints
- Files changed:
  - backend/src/main/java/com/futspring/backend/dto/CreatePeladaRequestDTO.java
  - backend/src/main/java/com/futspring/backend/dto/PeladaMemberDTO.java
  - backend/src/main/java/com/futspring/backend/dto/PeladaDetailResponseDTO.java
  - backend/src/main/java/com/futspring/backend/repository/PeladaRepository.java
  - backend/src/main/java/com/futspring/backend/service/PeladaService.java
  - backend/src/main/java/com/futspring/backend/controller/PeladaController.java
- **Learnings for future iterations:**
  - Authentication principal is email string — use `(String) authentication.getPrincipal()` in controllers
  - @Transactional is required on service methods that access lazy-loaded ManyToMany collections
  - Builder with @Builder.Default sets initialize properly so you can add to them before first save()
  - 403 is thrown as AppException(HttpStatus.FORBIDDEN, message) — caught by GlobalExceptionHandler
---

## 2026-03-20 - US-003
- Added `creator` field (@ManyToOne) to Pelada entity to track who created the pelada
- Updated `createPelada` in PeladaService to set creator on new peladas
- Added `addPlayer`, `removePlayer`, `setAdmin`, `searchUsers` methods to PeladaService
- Added `searchByUsernameOrEmail` JPQL query to UserRepository (case-insensitive, username OR email)
- Created AddPlayerRequestDTO and SetAdminRequestDTO request DTOs
- Created UserController at /api/v1/users with GET /search?q= endpoint
- Added POST /{id}/players, DELETE /{id}/players/{userId}, PUT /{id}/players/{userId}/admin to PeladaController
- Files changed:
  - backend/src/main/java/com/futspring/backend/entity/Pelada.java
  - backend/src/main/java/com/futspring/backend/repository/UserRepository.java
  - backend/src/main/java/com/futspring/backend/service/PeladaService.java
  - backend/src/main/java/com/futspring/backend/controller/PeladaController.java
  - backend/src/main/java/com/futspring/backend/controller/UserController.java
  - backend/src/main/java/com/futspring/backend/dto/AddPlayerRequestDTO.java
  - backend/src/main/java/com/futspring/backend/dto/SetAdminRequestDTO.java
- **Learnings for future iterations:**
  - Pelada entity has a `creator` field (ManyToOne User) - always check this when implementing creator-restricted operations
  - UserController is at /api/v1/users and uses PeladaService for searchUsers
  - UserRepository has `searchByUsernameOrEmail(@Param("q") String q)` JPQL query for case-insensitive search
  - 409 Conflict is HttpStatus.CONFLICT in AppException
---

## 2026-03-20 - US-004
- Created UpdatePeladaRequestDTO (all fields optional for partial update)
- Added `updatePelada`, `deletePelada`, `uploadPeladaImage` methods to PeladaService
- Added PUT /{id}, DELETE /{id}, POST /{id}/image endpoints to PeladaController
- Created FileController at /api/v1/files/{filename} serving files from uploads/ dir
- Updated SecurityConfig to permit public access to /api/v1/files/**
- Files changed:
  - backend/src/main/java/com/futspring/backend/dto/UpdatePeladaRequestDTO.java (new)
  - backend/src/main/java/com/futspring/backend/service/PeladaService.java
  - backend/src/main/java/com/futspring/backend/controller/PeladaController.java
  - backend/src/main/java/com/futspring/backend/controller/FileController.java (new)
  - backend/src/main/java/com/futspring/backend/config/SecurityConfig.java
- **Learnings for future iterations:**
  - File uploads use `@RequestParam("file") MultipartFile file` in controller (not @RequestBody)
  - `@Value("${app.uploads.dir:uploads}")` sets configurable uploads directory with default
  - File serving uses `UrlResource` from `org.springframework.core.io` - resolves from uploads dir
  - Delete endpoint should clear ManyToMany collections (members, admins) before deleting entity to avoid FK constraint violations
  - File serving endpoint should be permitAll() in SecurityConfig so images are publicly accessible
  - UpdatePeladaRequestDTO uses all nullable (boxed) types so null fields are ignored (partial update pattern)
---

## 2026-03-20 - US-005
- Created src/types/pelada.ts with PeladaMember, PeladaResponse, PeladaDetail interfaces
- Created src/api/peladas.ts with all pelada API functions using existing Axios client
- Files changed:
  - frontend/src/types/pelada.ts (new)
  - frontend/src/api/peladas.ts (new)
- **Learnings for future iterations:**
  - apiClient in client.ts automatically attaches Bearer token via interceptor in AuthContext.tsx
  - auth.ts pattern: functions return typed response.data directly; no manual error handling needed
  - uploadPeladaImage uses FormData with explicit Content-Type header for multipart
  - searchUsers uses axios params option: `{ params: { q } }` for query strings
---

## 2026-03-20 - US-006
- Replaced placeholder HomePage with real page fetching and displaying peladas grid
- Skeleton loading state (4 skeleton cards) shown while fetching
- Empty state message shown when no peladas
- Each pelada card shows image (or ⚽ placeholder), name, dayOfWeek, timeOfDay
- Cards link to /pelada/:id using React Router Link
- Files changed:
  - frontend/src/pages/HomePage.tsx
- **Learnings for future iterations:**
  - Skeleton cards use `animate-pulse` Tailwind class and `bg-muted` for placeholder blocks
  - Image path for uploaded files: `/api/v1/files/${pelada.image}` (not full URL)
  - `container max-w-5xl mx-auto px-4 py-8` is a good main content wrapper pattern
  - Use `Link` from react-router-dom for navigation cards (not `<a>` tags)
  - No browser testing tools available; manual browser verification needed
---

## 2026-03-20 - US-007
- Created CreatePeladaModal component with full form (name, dayOfWeek, timeOfDay, duration, address, reference, autoCreateDailyEnabled toggle, optional image upload)
- Added "Create Pelada" button to HomePage header that opens the modal
- On success: closes modal, refetches peladas list, shows success toast, navigates to /pelada/:id
- On error: shows error toast, keeps modal open
- Image file validated client-side (5MB limit, JPEG/PNG/WebP only) before upload
- Files changed:
  - frontend/src/components/CreatePeladaModal.tsx (new)
  - frontend/src/pages/HomePage.tsx
- **Learnings for future iterations:**
  - No @radix-ui/react-dialog installed — use fixed overlay pattern for modals (see CreatePeladaModal.tsx)
  - `<select>` needs manual Tailwind classes to match Input component styling
  - `uploadPeladaImage` is called after `createPelada` using the returned pelada.id
  - `onCreated` callback pattern allows parent to refetch data after modal success
  - No browser testing tools available; manual browser verification needed
---

## 2026-03-20 - US-008
- Created PeladaDetailPage at /pelada/:id
- Header: image banner (or ⚽ placeholder), name, dayOfWeek+timeOfDay, duration, address, reference
- Members section: avatar (initials fallback), username, position badge, star rating (★/☆), Admin badge
- 403 from API detected via err.response.status → shows Access Denied message (no crash)
- Skeleton loading state with pulse animation
- NavBar present at top
- Updated App.tsx to use PeladaDetailPage (lazy loaded) replacing the placeholder
- Files changed:
  - frontend/src/pages/PeladaDetailPage.tsx (new)
  - frontend/src/App.tsx
- **Learnings for future iterations:**
  - Error status check pattern: `err?.response?.status === 403` in catch block
  - initials avatar: `username.slice(0, 2).toUpperCase()` with `bg-muted` circle
  - Star rating: map array of length 5, show ★ for i < stars else ☆
  - Lazy-load new pages in App.tsx using `const X = lazy(() => import('./pages/X'))`
  - No browser testing tools available; manual browser verification needed
---

## 2026-03-20 - US-010
- Created EditPeladaModal component (mirrors CreatePeladaModal, pre-filled with current pelada values)
- Added Edit button (admin-only) and Delete Pelada button (creator-only) to PeladaDetailPage header area
- EditPeladaModal: calls updatePelada(), optionally uploadPeladaImage() if new image selected; toast on success/error; loading state on submit
- Delete confirmation dialog inline in PeladaDetailPage: warning text, loading state, navigates to /home on confirm
- Files changed:
  - frontend/src/components/EditPeladaModal.tsx (new)
  - frontend/src/pages/PeladaDetailPage.tsx
- **Learnings for future iterations:**
  - Edit modal pattern: initialize form state from pelada prop using `useState<UpdatePeladaData>({ ...pelada fields })`
  - Creator check uses `currentUser != null && creatorId === currentUser.id` (both must be non-null)
  - Delete dialog: set `setDeleting(false)` on error so user can retry; only navigate to /home on success
  - No browser testing tools available; manual browser verification needed
---

## 2026-03-20 - US-011
- Created Daily JPA entity with fields: id, pelada (ManyToOne), dailyDate (LocalDate), dailyTime (String), status (String, default SCHEDULED), createdAt
- Created DailyRepository extending JpaRepository with existsByPeladaAndDailyDate derived query
- Hibernate DDL auto creates the dailies table from the entity
- Files changed:
  - backend/src/main/java/com/futspring/backend/entity/Daily.java (new)
  - backend/src/main/java/com/futspring/backend/repository/DailyRepository.java (new)
- **Learnings for future iterations:**
  - Daily entity uses LocalDate (not LocalDateTime) for dailyDate field
  - DailyRepository derived query `existsByPeladaAndDailyDate` works because Spring Data JPA supports boolean return types for exists queries
  - @Builder.Default on `status` field ensures default value is set when using the builder pattern
---

## 2026-03-20 - US-009
- Added admin player management UI to PeladaDetailPage
- 'Add Player' button (admin only): opens AddPlayerDialog with debounced searchUsers() (300ms), shows results with 'Add' button, marks existing members; calls addPlayer() on select
- Each member row shows 'Make Admin'/'Remove Admin' and 'Remove' buttons (admin only, hidden for non-admins); creator rows hide both actions
- Confirmation dialog (ConfirmRemoveDialog) before removePlayer(); success/error toasts for all actions
- Backend PeladaDetailResponseDTO updated to include creatorId field
- Frontend PeladaDetail type updated with `creatorId: number | null`
- Files changed:
  - frontend/src/pages/PeladaDetailPage.tsx
  - frontend/src/types/pelada.ts
  - backend/src/main/java/com/futspring/backend/dto/PeladaDetailResponseDTO.java
- **Learnings for future iterations:**
  - `useCallback` on fetchPelada allows passing it as a dependency and calling it from event handlers
  - `useRef<ReturnType<typeof setTimeout>>` is correct type for debounce timers in TypeScript
  - Creator check: compare `member.id === pelada.creatorId`; need backend to expose creatorId
  - No browser testing tools available; manual browser verification needed
---

## 2026-03-20 - US-012
- Added @EnableScheduling to FutSpringApplication
- Added findByAutoCreateDailyEnabledTrue() derived query to PeladaRepository
- Created DailySchedulerService with hourly @Scheduled(cron = "0 0 * * * *") job
- Job finds all peladas with autoCreateDailyEnabled=true, calculates next dayOfWeek occurrence, creates Daily if within 24h and not already existing
- Files changed:
  - backend/src/main/java/com/futspring/backend/FutSpringApplication.java
  - backend/src/main/java/com/futspring/backend/repository/PeladaRepository.java
  - backend/src/main/java/com/futspring/backend/service/DailySchedulerService.java (new)
- **Learnings for future iterations:**
  - @EnableScheduling must be on a @SpringBootApplication or @Configuration class; added to FutSpringApplication
  - @Scheduled cron format for Spring is 6 fields: seconds minutes hours day month weekday (not 5 like Unix cron)
  - getNextOccurrence iterates from today until the target DayOfWeek is found (max 7 days)
  - DayOfWeek.valueOf() expects uppercase English day names (MONDAY, TUESDAY, etc.) — matches the dayOfWeek field format
  - Duration.between(now, nextOccurrenceDateTime).toHours() returns hours; check 0–24 for "within next 24 hours"
---
