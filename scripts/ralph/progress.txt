# Ralph Progress Log
Started: Sat Mar 21 09:15:01 PDT 2026
---

## Codebase Patterns
- shadcn components live in `frontend/src/components/ui/` and are built manually from Radix UI primitives
- All shadcn animation classes (`animate-in`, `animate-out`, `fade-in-0`, etc.) require `tailwindcss-animate` plugin in tailwind.config.js
- The project uses `@radix-ui/react-slot`, `class-variance-authority`, `clsx`, `tailwind-merge` (via `cn()` in lib/utils.ts)
- CSS variables for colors are defined elsewhere (index.css), following shadcn convention
- Branch for this PRD: `ralph/frontend-redesign`
- Button variants live in `frontend/src/components/ui/button-variants.ts` (separate from button.tsx) — add new variants there

## 2026-03-21 - US-001
- Created shadcn UI components manually: avatar.tsx, badge.tsx, skeleton.tsx, separator.tsx, dialog.tsx, dropdown-menu.tsx, sheet.tsx
- Installed missing npm packages: @radix-ui/react-avatar, @radix-ui/react-dialog, @radix-ui/react-dropdown-menu, @radix-ui/react-separator, tailwindcss-animate
- Added `tailwindcss-animate` plugin to tailwind.config.js (required for shadcn animation data attributes)
- Existing components (button, card, input, label, tabs) left untouched
- tailwind.config.js darkMode: ["class"] was already set correctly
- **Learnings for future iterations:**
  - Sheet component reuses @radix-ui/react-dialog (not a separate package)
  - Dialog and Sheet both need tailwindcss-animate for their open/close animations
  - All created components follow standard shadcn patterns and work with existing CSS variable system
---

## 2026-03-21 - US-003
- Rewrote NavBar.tsx with sticky top bar, backdrop blur, Trophy icon wordmark linking to /home
- Center desktop nav link 'Home' with active green underline via useLocation
- Right side: dark mode toggle (Sun/Moon icons via MutationObserver) + user avatar DropdownMenu
- DropdownMenu contains non-clickable user info header (username + email), 'View Profile' link, 'Logout'
- Avatar uses shadcn Avatar with image or initials fallback
- Files changed: frontend/src/components/NavBar.tsx
- **Learnings for future iterations:**
  - DropdownMenu closes on outside click/Escape automatically (Radix UI behavior)
  - MutationObserver on document.documentElement is the right approach to sync icon with dark mode class changes from external sources
  - DropdownMenuLabel is used for non-clickable header content in the avatar dropdown
---

## 2026-03-21 - US-004
- Added localStorage theme persistence: read on app mount in main.tsx, write on toggle in NavBar.tsx
- main.tsx: reads localStorage.getItem('theme') before React renders, applies/removes 'dark' class to avoid flash
- NavBar.tsx: handleToggleDark now also calls localStorage.setItem('theme', nowDark ? 'dark' : 'light')
- Icons (Sun/Moon) and aria-label were already correct from US-003
- Files changed: frontend/src/main.tsx, frontend/src/components/NavBar.tsx
- **Learnings for future iterations:**
  - Theme init belongs before createRoot() in main.tsx to avoid flash-of-wrong-theme
  - NavBar's MutationObserver from US-003 still works correctly — it just keeps local state in sync
  - Manual browser verification needed (no browser tools available)
---

## 2026-03-21 - US-005
- Added mobile hamburger menu (Menu lucide icon, block sm:hidden) that opens a shadcn Sheet (side='right')
- Desktop: dark mode toggle + avatar DropdownMenu remain but are now hidden on mobile (hidden sm:flex / hidden sm:inline-flex)
- Sheet drawer contains: Home nav link (SheetClose), dark mode toggle (closes sheet on click), View Profile link (SheetClose), Logout button (closes sheet + navigates)
- Added `sheetOpen` state to control Sheet open/close programmatically
- Files changed: frontend/src/components/NavBar.tsx
- **Learnings for future iterations:**
  - Sheet uses controlled open/onOpenChange pattern for programmatic close on item click
  - SheetClose asChild wraps Link elements to auto-close sheet on navigation
  - For non-Link items (dark mode, logout), manually call setSheetOpen(false) before the action
  - Desktop controls use `hidden sm:flex` / `hidden sm:inline-flex`; mobile hamburger uses `block sm:hidden`
  - Manual browser verification needed (no browser tools available)
---

## 2026-03-21 - US-006
- Created ErrorBoundary.tsx (React class component, getDerivedStateFromError + componentDidCatch, renders 'Something went wrong' + Reload button)
- Created ScrollToTop.tsx (useEffect + useLocation, calls window.scrollTo(0,0) on pathname change, rendered in App.tsx)
- Created NotFoundPage.tsx (football ⚽ themed 404, Back to Home button using Button component)
- Updated App.tsx: each lazy route wrapped in per-route ErrorBoundary, ScrollToTop rendered at top, catch-all `path="*"` route for 404
- Updated index.css: added @keyframes fadeIn + .page-enter utility class (animation: fadeIn 150ms ease-in)
- Added `page-enter` class to root divs of: HomePage, AuthPage, PeladaDetailPage, DailyDetailPage; ProfilePage fragment replaced with div
- Files changed: ErrorBoundary.tsx, ScrollToTop.tsx, NotFoundPage.tsx, App.tsx, index.css, all 5 page files
- **Learnings for future iterations:**
  - ProfilePage uses `<>` fragment as root — must replace with `<div>` to add animation class
  - ScrollToTop must be inside Router context (it's in App.tsx which is inside BrowserRouter from main.tsx)
  - React class component ErrorBoundary requires `React.ErrorInfo` type import from 'react' for componentDidCatch
  - The `page-enter` CSS class approach is cleaner than inline Tailwind arbitrary values for reuse across pages
  - Manual browser verification needed (no browser tools available)
---

## 2026-03-21 - US-002
- Added `gradient` variant to `frontend/src/components/ui/button-variants.ts`
- Applied `variant="gradient"` to: "Create Pelada" (HomePage + CreatePeladaModal), "Sort Teams", "Confirm Attendance", "Start Session" (DailyDetailPage)
- DailyDetailPage used raw `<button>` elements — imported `Button` component and replaced relevant buttons
- Files changed: button-variants.ts, HomePage.tsx, CreatePeladaModal.tsx, DailyDetailPage.tsx
- **Learnings for future iterations:**
  - DailyDetailPage uses raw `<button>` elements (not the Button component) for most actions — must import Button when using variants
  - "Create Pelada" button appears in both HomePage (header) and CreatePeladaModal (submit) — both need updating
  - Typecheck had no issues; the `gradient` variant integrates cleanly with existing CVA setup
---

## 2026-03-21 - US-007
- Created LandingPage.tsx: min-h-screen green gradient hero, auth redirect (Navigate to /home if token exists), headline, subheadline, two CTA buttons ('Get Started' → /auth?tab=register, 'Sign In' → /auth), decorative football emoji (hidden on mobile)
- Updated App.tsx: replaced inline placeholder with lazy-loaded LandingPage wrapped in ErrorBoundary
- Fixed pre-existing ErrorBoundary.tsx type error: `ReactNode` must use type-only import (`import type`) when verbatimModuleSyntax is enabled
- Files changed: frontend/src/pages/LandingPage.tsx (new), frontend/src/App.tsx, frontend/src/components/ErrorBoundary.tsx
- **Learnings for future iterations:**
  - Auth redirect in landing page uses `useAuth()` (token check) + `<Navigate to="/home" replace />` before rendering — no flash since token is read synchronously from localStorage
  - `verbatimModuleSyntax` is enabled in tsconfig — all type imports must use `import type { ... }` syntax
  - LandingPage doesn't need NavBar (it's a public marketing page with its own full-screen gradient)
  - Manual browser verification needed (no browser tools available)
---

## 2026-03-21 - US-008
- Added Features section (4 cards in responsive grid: 1 col mobile, 2 tablet, 4 desktop) with lucide icons (Calendar, Users, BarChart2, MessageSquare) and bg-green-50 dark:bg-green-950/20 background
- Added How It Works section with 3 numbered green circle steps
- Added Footer with dark background (bg-gray-900), 'FutSpring © 2025' text, and Sign In link
- Restructured LandingPage to wrap all sections in a single root div (hero + features + how-it-works + footer)
- Files changed: frontend/src/pages/LandingPage.tsx
- **Learnings for future iterations:**
  - LandingPage is a full-page marketing page without NavBar — all sections must be self-contained
  - Used `page-enter` class on outermost div; hero section's `min-h-screen` stays on the hero div, not the root
  - Manual browser verification needed (no browser tools available)
---

## 2026-03-21 - US-010
- Added gradient overlay (`bg-gradient-to-t from-black/70 to-transparent`) + pelada name in white text over banner
- Moved Edit/Delete buttons to banner top-right as icon-only buttons (Pencil, Trash2) with bg-black/40 background
- Wrapped Members, Sessions, Ranking sections in shadcn Tabs (defaultValue="members")
- Chat sidebar: `hidden lg:block` (desktop-only); mobile gets fixed bottom-right floating MessageCircle button
- Mobile chat panel toggles as a fixed 400px panel above the floating button
- Removed old pelada name heading and old inline Edit/Delete text buttons from content area
- Files changed: frontend/src/pages/PeladaDetailPage.tsx
- **Learnings for future iterations:**
  - Tabs components from `../components/ui/tabs` use `Tabs`, `TabsList`, `TabsTrigger`, `TabsContent`
  - Section headers (h2) can be dropped inside tabs since TabsTrigger acts as the header
  - Mobile chat: `lg:hidden` on floating button hides it on desktop; `hidden lg:block` on sidebar hides it on mobile
  - `isCurrentUserAdmin` is derived from `pelada.members` + `currentUser.id`; `isCurrentUserCreator` uses `pelada.creatorId`
  - Manual browser verification needed (no browser tools available)
---

## 2026-03-21 - US-011
- Replaced inline 'Make Admin' / 'Remove' buttons with a MoreVertical (3-dot) icon button per member row
- Icon button opens a shadcn DropdownMenu with 'Make Admin' / 'Remove Admin' (toggled) and 'Remove from Pelada'
- Member names changed from `<span>` to `<Link to={/profile/${member.id}}>` with hover underline
- DropdownMenu uses existing `handleToggleAdmin` and `setConfirmRemoveMember` — no backend changes
- 3-dot button shown only when `isCurrentUserAdmin && !isCreator`
- Files changed: frontend/src/pages/PeladaDetailPage.tsx
- **Learnings for future iterations:**
  - `DropdownMenuContent` with `align="end"` positions the menu to the right of the trigger button
  - `DropdownMenuItem` supports `disabled` prop natively (Radix UI)
  - For destructive menu items use `className="text-destructive focus:text-destructive"` — no separate variant needed
  - Manual browser verification needed (no browser tools available)
---

## 2026-03-21 - US-009
- Updated HomePage with polished pelada cards grid
- Card changes: `rounded-xl border shadow-sm`, `font-bold text-lg` name, green gradient ⚽ placeholder, member count Badge, next session date
- Heading: added `tracking-tight`, button renamed to "+ New Pelada"
- Grid: `grid-cols-1 md:grid-cols-2 lg:grid-cols-3` (breakpoints shifted right from previous sm/md)
- Skeleton: updated to use shadcn `Skeleton` component (3 cards, matching card shape)
- Empty state: updated text to "You're not in any pelada yet. Create one to get started." + green gradient button
- Next session: fetches dailies for all peladas in parallel via `Promise.all(getDailiesForPelada(...))`, filters to SCHEDULED/CONFIRMED, picks earliest by date sort; shows "No upcoming session" if none
- Files changed: frontend/src/pages/HomePage.tsx
- **Learnings for future iterations:**
  - `PeladaResponse` already has `memberCount` — no extra API call needed
  - `DailyListItem.dailyDate` is a string in ISO format; `.localeCompare()` sorts correctly
  - `nextSession === undefined` (loading state) shows "…"; `null` shows "No upcoming session"; string shows the date
  - Manual browser verification needed (no browser tools available)
---

## 2026-03-21 - US-013
- Added `tracking-tight` to date h1 title
- Replaced custom `StatusBadge` span with shadcn `Badge` component using status-specific className for color
- Added pulsing green dot + "Live" text indicator for IN_COURSE status
- Changed time subtitle to `text-sm text-muted-foreground`
- Merged two separate admin action sections (SCHEDULED/CONFIRMED and IN_COURSE) into single styled wrapper: `rounded-xl border p-3 flex flex-wrap gap-2 bg-muted/40`
- 'Confirm Daily': yellow gradient via inline className (`bg-gradient-to-br from-yellow-400 to-amber-500`)
- 'Start Session': existing `variant="gradient"`
- 'Cancel Daily': `variant="outline"` with `border-destructive text-destructive` classes
- 'Enter Results / Finalize' and 'Finalize Daily': `variant="gradient"`
- Files changed: frontend/src/pages/DailyDetailPage.tsx
- **Learnings for future iterations:**
  - shadcn Badge accepts `className` for per-status color overrides — no need for a custom variant
  - The two admin action areas (SCHEDULED/CONFIRMED vs IN_COURSE) were previously separate divs — now unified in one `rounded-xl border bg-muted/40` container
  - Yellow gradient for Confirm Daily uses inline className on Button, not a new button variant
  - Manual browser verification needed (no browser tools available)
---

## 2026-03-21 - US-014
- Confirmed players section: shadcn Avatar (image or initials fallback) + player name as Link to /profile/:id + position Badge + star rating
- Section header updated to show '(N confirmed)' count
- Teams section: TEAM_HEADER_COLORS array cycles bg-green-100, bg-blue-100, bg-purple-100, bg-orange-100 with dark variants
- Team cards: colored header bar via `overflow-hidden` on card + colored header div; players show shadcn Avatar + name + stars
- Move/Swap button: `<ArrowLeftRight className="h-3 w-3" />` icon button styled with border + rounded
- Team cards grid: `grid-cols-1 sm:grid-cols-2` — single column on mobile
- Files changed: frontend/src/pages/DailyDetailPage.tsx
- **Learnings for future iterations:**
  - PlayerAvatar custom component was replaced with shadcn Avatar in both confirmed players and team cards sections
  - TEAM_HEADER_COLORS array is defined at module level before the TeamsSection component
  - `overflow-hidden` on the team card container is required for the colored header to not overflow card border-radius
  - Manual browser verification needed (no browser tools available)
---

## 2026-03-21 - US-012
- Added `STATUS_BORDER` map alongside existing `STATUS_COLORS` for border-l-4 left border color per session status
- Session rows: added `border-l-4 ${STATUS_BORDER[daily.status]}` class to Link — SCHEDULED=yellow, CONFIRMED=green, IN_COURSE=blue, FINISHED=gray, CANCELED/CANCELLED=red
- Sessions empty state: replaced plain text with football emoji + 'No sessions scheduled yet.'
- Ranking table: added '#' rank column as first column in thead; tbody rows use index to show 🥇🥈🥉 for top 3, numeric rank for rest
- Sorting logic (rankingSort state + sortedRanking) unchanged — medals reflect sorted order
- Files changed: frontend/src/pages/PeladaDetailPage.tsx
- **Learnings for future iterations:**
  - The codebase uses `CANCELED` (one L) not `CANCELLED` (two L's) — added both keys to STATUS_BORDER for safety
  - `sortedRanking.map((row, idx) => ...)` — `idx` is the post-sort position, which is correct for medals
  - Manual browser verification needed (no browser tools available)
---

## 2026-03-21 - US-016
- Added green top border to StatCard: `border border-t-2 border-t-green-500 rounded-lg`
- Added Peladas section below Puskas Award History: fetches `getMyPeladas()` + `getPelada(id)` for each, filters to those where profile user is in members list
- Pelada cards: 2 col mobile (`grid-cols-2`) / 3 col desktop (`md:grid-cols-3`); banner image or green gradient ⚽ placeholder; clicking → /pelada/:id
- Skeleton: 3 skeleton cards (shadcn Skeleton) while loading
- Empty state: 'Not in any peladas yet.'
- Fixed EditProfileModal dark mode: added `text-foreground` to username input and position select so text isn't invisible in dark mode
- Files changed: frontend/src/pages/ProfilePage.tsx
- **Learnings for future iterations:**
  - No backend endpoint for "get peladas for user X" — workaround: fetch `getMyPeladas()` + `getPelada(id)` for each, then filter by profile userId in members; shows only shared peladas when viewing another user's profile
  - ProfilePage uses a custom `SkeletonBlock` for its own profile skeleton (not shadcn) — keep both; use shadcn `Skeleton` for new sections
  - StatCard's border classes must include `border` (for sides) AND `border-t-2 border-t-green-500` — Tailwind's `border-t-2` overrides top but doesn't auto-add the other sides
  - Manual browser verification needed (no browser tools available)
---

## 2026-03-21 - US-015
- Champion photo moved to top of FINISHED section as full-width hero image (`-mx-4 sm:mx-0 w-full sm:rounded-xl object-cover max-h-80`)
- Admin upload button/section kept at bottom of finished block (no image shown there, just the upload action)
- Awards styled as trophy cards with `border-2` and inline `style={{ borderColor: '#FFD700' }}` (gold) / `#C0C0C0` (silver)
- Medal icons 🥇🥈🥉 added to league table position column (via ternary on `entry.position`)
- Medal icons added to player stats first column using `idx` from `.map((stat, idx) => ...)`
- Player stats section collapsed by default: added `playerStatsExpanded` state (false) + toggle button
- IN_COURSE CTA card added when `daily.status === 'IN_COURSE' && daily.matches.length === 0`:
  - Admin: prominent card with `animate-pulse` absolute border overlay (green) + "Enter Results" gradient button
  - Non-admin: simple "Session is live" card with pulsing dot
- Files changed: frontend/src/pages/DailyDetailPage.tsx
- **Learnings for future iterations:**
  - Pulsing border effect: use `absolute inset-0 rounded-xl border-2 border-green-500 animate-pulse pointer-events-none` overlay on a `relative` container — avoids pulsing content opacity
  - For inline color values outside Tailwind palette (like `#FFD700`, `#C0C0C0`), use `style={{ borderColor: '...' }}` prop
  - Champion hero section uses negative horizontal margin `-mx-4 sm:mx-0` for full-bleed effect on mobile within a padded container
  - Manual browser verification needed (no browser tools available)
---
