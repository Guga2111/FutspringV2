## Codebase Patterns
- Frontend custom hooks live at `frontend/src/components/<feature>/hooks/<hookName>.ts`; the hooks directory is created on first use (not pre-existing)
- Custom data-fetching hooks expose individual refetch functions (e.g., `refetchPelada`, `refetchDailies`) in addition to a combined `refetch` so callers can trigger targeted refreshes
- When extracting a service, the delegation stub in the original service must @Transactional-annotate wrapper methods and call the sub-service; the original service's tests become delegation tests (mock sub-service → verify call), and a new test file covers the real logic
- `DailyAttendanceService` is in the same package as `DailyService` so clearAttendees (package-private) is accessible to future orchestration code in that package
- Backend services use `@Service` + `@RequiredArgsConstructor` + `private final` constructor injection (no @Autowired field injection)
- AppException(HttpStatus, message) is the standard error pattern
- Tests use `new ServiceClass(repo1, repo2, ...)` with Mockito `@Mock` fields — no Spring context
- When adding a new service dependency, update ALL test files that `new` the affected service class
- `@Value("${app.uploads.dir:uploads}")` fields cannot coexist with `@RequiredArgsConstructor` if you want them injected — use `@Value` on a non-final field instead, or move to a config bean
- File upload logic uses: 5MB size check → MIME check (jpeg/png/webp) → UUID filename → Files.copy → return filename; IOException → AppException(500, "Failed to store file")
- UserAuthenticationHelper lives in `com.futspring.backend.helper` and delegates user lookup to avoid repeating the findByEmail pattern; services that removed their userRepository dependency (ChatService, RankingService, AwardsService) drop the repo from their imports but DailyService/PeladaService retain it for other uses
- Tests that check user-not-found now mock `userAuthHelper.getAuthenticatedUser(email)` to throw `AppException(NOT_FOUND)` instead of returning `Optional.empty()` from userRepository — and assert NOT_FOUND (not UNAUTHORIZED)
- `DailyDTOMapper` is a `@Component` (not `@Service`) that holds `buildTeamDTO` — injected into both `DailyTeamManagementService` and `DailyService` (for `getDailyDetail`); use `dailyDTOMapper::buildTeamDTO` method reference, never `this::buildTeamDTO`
- `DailyTeamManagementService` and `DailyDTOMapper` live in the same `service` package as `DailyService`; `clearTeams` is package-private (no visibility modifier)
- `DailyController` injects all 4 service beans (DailyService, DailyAttendanceService, DailyTeamManagementService, DailyResultsService); `finalizeDaily` and `populateFromMessage` routes call the results sub-service then `dailyService.getDailyDetail` because the sub-service returns void
- `deleteDaily` orchestration order: clearResults → clearTeams → clearAttendees → dailyRepository.delete (preserves referential integrity)

---

## 2026-04-10 - US-001
- Initialized prd.json from tasks/prd-codebase-refactoring.md (13 user stories)
- Created FileUploadService at backend/src/main/java/com/futspring/backend/service/FileUploadService.java
  - uploadImage(MultipartFile): String — validates 5MB limit + MIME type, generates UUID filename, saves to uploadsDir
  - deleteImage(String): void — best-effort delete, no-ops if file absent
- Updated DailyService: injected FileUploadService as first constructor param, uploadChampionImage delegates to fileUploadService.uploadImage; removed inline upload logic and unused imports (@Value, UUID, IOException, Files, Path, Paths)
- Updated PeladaService: injected FileUploadService as third constructor param, uploadPeladaImage delegates to fileUploadService.uploadImage + deleteImage; removed deleteOldFile() helper and unused imports
- Updated UserService: injected FileUploadService as second constructor param, uploadUserImage/uploadBackgroundImage delegate to fileUploadService.uploadImage + deleteImage; removed storeFile() and deleteOldFile() helpers and unused imports
- Updated test constructors in: DailyServiceTest, PeladaServiceImageTest, PeladaServiceTest, UserServiceTest, UserServiceProfileTest — all now pass mock FileUploadService
- Updated test assertions in PeladaServiceImageTest and UserServiceTest to verify mock interactions instead of real file I/O
- Files changed: FileUploadService.java (new), DailyService.java, PeladaService.java, UserService.java, DailyServiceTest.java, PeladaServiceImageTest.java, PeladaServiceTest.java, UserServiceTest.java, UserServiceProfileTest.java, scripts/ralph/prd.json (new), scripts/ralph/progress.txt (new)
- **Learnings for future iterations:**
  - Java is not installed in this environment — cannot run ./mvnw compile or ./mvnw test; compile correctness verified by manual review
  - The constructor arg order matters: @RequiredArgsConstructor generates constructor in field declaration order — FileUploadService was added first in DailyService so it's the first arg
  - PeladaServiceTest and UserServiceProfileTest also direct-instantiate their services, so both needed to be updated in addition to PeladaServiceImageTest and UserServiceTest
  - When refactoring file upload tests, the old tests verified real file I/O via @TempDir; after mocking FileUploadService, the tests now verify the mock was called (verify()) instead of file existence (assertThat(path).exists())
---

## 2026-04-10 - US-002
- Created `UserAuthenticationHelper` @Component at `backend/src/main/java/com/futspring/backend/helper/UserAuthenticationHelper.java`
  - `getAuthenticatedUser(String email): User` — delegates to `userRepository.findByEmail(email).orElseThrow(AppException(NOT_FOUND, "User not found"))`
- Updated DailyService: added `UserAuthenticationHelper` as 2nd constructor field; replaced 17 findByEmail+orElseThrow calls; kept `userRepository` for `findAllById` in `populateFromMessage`
- Updated PeladaService: added `UserAuthenticationHelper` as 4th constructor field; replaced 9 findByEmail+orElseThrow calls; kept `userRepository` for `findById` and `searchByUsernameOrEmail`
- Updated ChatService: replaced `UserRepository` with `UserAuthenticationHelper` entirely (only used for findByEmail)
- Updated RankingService: replaced `UserRepository` with `UserAuthenticationHelper` entirely
- Updated AwardsService: replaced `UserRepository` with `UserAuthenticationHelper` entirely
- Updated test files: DailyServiceTest, PeladaServiceTest, PeladaServiceImageTest, ChatServiceTest, RankingServiceTest, AwardsServiceTest
  - Added `@Mock UserAuthenticationHelper userAuthHelper`; updated constructor calls; replaced userRepository.findByEmail stubs with userAuthHelper.getAuthenticatedUser stubs; updated "callerNotFound" assertions from UNAUTHORIZED to NOT_FOUND
- Files changed: UserAuthenticationHelper.java (new), DailyService.java, PeladaService.java, ChatService.java, RankingService.java, AwardsService.java + 6 test files + prd.json + progress.txt
- **Learnings for future iterations:**
  - `@RequiredArgsConstructor` constructor arg order follows field declaration order — add new helper field in the position that matches where you insert it in tests
  - When a service only uses `userRepository.findByEmail` (ChatService, RankingService, AwardsService), the whole userRepository dependency can be dropped after extracting to the helper
  - Test stubs for "user not found" now use `.thenThrow(new AppException(HttpStatus.NOT_FOUND, ...))` directly on the helper mock, not `.thenReturn(Optional.empty())` on userRepository
  - Java is not installed in this environment — cannot run ./mvnw compile; correctness verified by manual review of all changed files
---

## 2026-04-10 - US-003
- Created `DailyAttendanceService` @Service at `backend/src/main/java/com/futspring/backend/service/DailyAttendanceService.java`
  - `confirmAttendance(Long id, String email): DailyListItemDTO` — membership check, locked-status guard, duplicate guard, adds player
  - `disconfirmAttendance(Long id, String email): DailyListItemDTO` — locked-status guard, not-confirmed guard, removes player
  - `adminConfirmAttendance(Long dailyId, Long targetUserId, String email): DailyListItemDTO` — admin check, target lookup/member check, locked guard, conflict guard
  - `adminDisconfirmAttendance(Long dailyId, Long targetUserId, String email): DailyListItemDTO` — admin check, target lookup/member check, locked guard, not-confirmed guard
  - `clearAttendees(Daily daily): void` — package-private, clears confirmed players and saves
  - Dependencies: UserAuthenticationHelper, DailyRepository, UserRepository
- Updated DailyService: added `DailyAttendanceService dailyAttendanceService` as 3rd field (after userAuthHelper); replaced 4 attendance methods with 1-line delegation stubs; LOCKED_STATUSES kept (still used by sortTeams/swapPlayers)
- Updated DailyServiceTest: added `@Mock DailyAttendanceService`; updated constructor call (14 args); replaced 13 attendance-logic tests with 4 delegation tests; added missing `ReflectionTestUtils` import
- Created DailyAttendanceServiceTest: 21 unit tests covering all paths for all 5 methods
- Files changed: DailyAttendanceService.java (new), DailyService.java, DailyServiceTest.java, DailyAttendanceServiceTest.java (new), prd.json, progress.txt
- **Learnings for future iterations:**
  - When extracting a service, the original service's @Transactional wrapper delegates with a single return statement; the original tests become delegation tests and a new test file covers the real logic
  - `DailyAttendanceService` has its own `LOCKED_STATUSES` constant (duplicated from DailyService); DailyService keeps its copy for sortTeams/swapPlayers (US-004 will move those)
  - The `clearAttendees` method is package-private (no modifier) — accessible within `com.futspring.backend.service` package for future orchestration
  - DailyServiceTest was missing `import org.springframework.test.util.ReflectionTestUtils` — added it as part of this update
  - Java is not installed in this environment — cannot run ./mvnw compile; correctness verified by manual review
---

## 2026-04-11 - US-004
- `DailyTeamManagementService` and `DailyDTOMapper` were already created in a prior partial run (US-003 commit left them untracked). This iteration completed the wiring.
- Fixed `DailyService.java`: changed `this::buildTeamDTO` (compile error — no such private method) to `dailyDTOMapper::buildTeamDTO` at line 664 in `getDailyDetail`
- Updated `DailyServiceTest.java`: added `@Mock DailyTeamManagementService dailyTeamManagementService` and `@Mock DailyDTOMapper dailyDTOMapper`; updated `new DailyService(...)` constructor call to include both new mocks; replaced 14 inline `sortTeams`/`swapPlayers` logic tests with 4 delegation tests (2 per method)
- Created `DailyTeamManagementServiceTest.java` with 12 unit tests covering:
  - `sortTeams` error paths: callerNotAdmin, locked statuses (IN_COURSE/FINISHED/CANCELED), tooFewPlayers, tooManyPlayers, dailyNotFound, callerNotFound
  - `sortTeams` success path: verifies old teams deleted, 2 new teams created
  - `swapPlayers` swap logic: verifies players exchanged between teams and both teams saved
  - `swapPlayers` error paths: callerNotAdmin, lockedStatus, player1NotOnTeam, player2NotOnTeam, dailyNotFound, callerNotFound
- Files changed: DailyService.java (1 line fix), DailyServiceTest.java (mocks + constructor + delegation tests), DailyTeamManagementServiceTest.java (new), scripts/ralph/prd.json, scripts/ralph/progress.txt
- **Learnings for future iterations:**
  - When a prior run creates files but doesn't commit them (they show as `??` in git status), check the implementation for any compile-breaking gaps before assuming it's complete
  - `DailyDTOMapper` must be injected into any service that needs `buildTeamDTO` — including the retained `getDailyDetail` in `DailyService`
  - `DailyTeamManagementService.LOCKED_STATUSES` is its own copy; `DailyService` no longer needs that constant after US-006 removes the remaining stubs
---

## 2026-04-11 - US-005
- Created `DailyResultsService` @Service at `backend/src/main/java/com/futspring/backend/service/DailyResultsService.java`
  - `submitResults(Long id, List<MatchResultDTO> results, String email): List<MatchDTO>` — validates admin, status, team IDs, player stats totals, persists matches+stats, calls persistLiveLeagueTable
  - `finalizeDaily(Long id, List<Long> puskasIds, List<Long> wiltballIds, String email): void` — validates admin, status, matches; computes UserDailyStats/LeagueTable/Awards/Ranking/Stats; marks FINISHED (no getDailyDetail call)
  - `uploadChampionImage(Long id, MultipartFile file, String email): DailyListItemDTO` — validates admin, FINISHED status, delegates to fileUploadService
  - `populateFromMessage(Long id, PopulateDailyRequestDTO req, String email): void` — validates admin, status; wipes existing teams/matches; creates teams+matches+stats; sets IN_COURSE (no getDailyDetail call)
  - `clearResults(Daily daily, Pelada pelada): void` — package-private; deletes awards, userDailyStats, playerMatchStats, matches, leagueEntries; recalculates Ranking+Stats if was finished
  - `persistLiveLeagueTable(Daily, List<Team>, List<Match>): List<LeagueTableEntry>` — private; recalculates league table from scratch
  - `distributeStats(...)` — private; redistributes player goals/assists across matches
- Updated `DailyService.java`: added `DailyResultsService dailyResultsService` as 5th field; replaced 4 method bodies with delegation stubs; `finalizeDaily` and `populateFromMessage` call getDailyDetail after delegating; removed `persistLiveLeagueTable` and `distributeStats` private methods; cleaned up 3 unused imports (Collection, Comparator, HashSet)
- Updated `DailyServiceTest.java`: added `@Mock DailyResultsService dailyResultsService`; updated constructor call (18 args); added 8 new delegation tests for all 4 delegated methods
- Created `DailyResultsServiceTest.java` with 10 unit tests covering:
  - `submitResults` error paths: callerNotAdmin, callerNotFound, dailyNotFound, invalidStatus, invalidTeamIds
  - `submitResults` success: verifies MatchDTOs returned with correct scores
  - `finalizeDaily` error paths: callerNotAdmin, callerNotFound, dailyNotFound, invalidStatus, noMatches, invalidPuskasWinner
  - `finalizeDaily` success: verifies daily marked FINISHED, Ranking+Stats saved
- Files changed: DailyResultsService.java (new), DailyService.java, DailyServiceTest.java, DailyResultsServiceTest.java (new), prd.json, progress.txt
- **Learnings for future iterations:**
  - `finalizeDaily` and `populateFromMessage` return void in DailyResultsService to avoid circular dependency; DailyService delegation stubs call `getDailyDetail` after the sub-service returns
  - `clearResults` consolidates the results-teardown logic from `deleteDaily` — US-006 should replace `deleteDaily`'s inline deletion code with a call to `dailyResultsService.clearResults(daily, pelada)` + team/attendance teardown
  - `DailyResultsService` takes all 12 repositories in its constructor (in declaration order); when writing tests, pass them in the same order as the field declarations
  - `distributeStats` is a stateful private method (uses Collections.shuffle); tests for `populateFromMessage` would require mocking randomness — unit tests for the paths suffice, `distributeStats` logic is well-isolated
  - Java is not installed in this environment — cannot run ./mvnw compile; correctness verified by manual review
---

## 2026-04-11 - US-006
- Slimmed `DailyService` to 5 lifecycle methods: `createDaily`, `getDailiesForPelada`, `updateStatus`, `deleteDaily`, `getDailyDetail` (253 lines, under 300)
- Removed 12 delegation stubs from `DailyService` (confirmAttendance, disconfirmAttendance, adminConfirmAttendance, adminDisconfirmAttendance, sortTeams, swapPlayers, updateTeamName, updateTeamColor, submitResults, finalizeDaily, uploadChampionImage, populateFromMessage)
- Removed no-longer-needed constructor fields from `DailyService`: FileUploadService, UserRepository, StatsRepository, RankingRepository
- Refactored `deleteDaily` to delegate teardown in correct order: `dailyResultsService.clearResults` → `dailyTeamManagementService.clearTeams` → `dailyAttendanceService.clearAttendees` → `dailyRepository.delete`
- Updated `DailyController` to inject `DailyAttendanceService`, `DailyTeamManagementService`, `DailyResultsService` directly; `finalizeDaily` and `populateFromMessage` now call results service then `dailyService.getDailyDetail`
- Updated `DailyServiceTest`: removed 4 mock fields (FileUploadService, UserRepository, StatsRepository, RankingRepository); updated constructor; removed 14 delegation tests; added 4 deleteDaily tests (success, callerNotAdmin, dailyNotFound, callerNotFound)
- Created `docs/adr/001-daily-service-split.md` with Context, Decision, Consequences
- Files changed: DailyService.java, DailyController.java, DailyServiceTest.java, docs/adr/001-daily-service-split.md (new), prd.json, progress.txt
- **Learnings for future iterations:**
  - When DailyService loses its delegation stubs, DailyController must inject the sub-services directly — `finalizeDaily` and `populateFromMessage` need a two-step call (sub-service → getDailyDetail) because the results service returns void
  - `getDailyDetail` still reads from 6 repositories (teams, matches, playerMatchStats, userDailyStats, leagueTableEntries, awards) — intentional for the read-model DTO; a future `DailyQueryService` could extract it
  - Strict Mockito fails on unnecessary stubs; don't stub `playerMatchStatRepository.findByMatchInWithUser` in getDailyDetail tests since Mockito returns an empty list by default for List return types
  - Java is not installed in this environment — cannot run ./mvnw compile; correctness verified by manual review
---

## 2026-04-11 - US-007
- Created `usePeladaDetail(id: string | undefined)` hook at `frontend/src/components/pelada/hooks/usePeladaDetail.ts`
  - Consolidates four data-fetching concerns: pelada (loading, accessDenied, error), dailies (dailiesLoading), ranking (rankingLoading), awards (awardsLoading)
  - Each concern has its own loading flag via separate useCallback + useEffect pairs
  - Returns `{ pelada, dailies, ranking, awards, loading, rankingLoading, dailiesLoading, awardsLoading, accessDenied, error, refetch, refetchPelada, refetchDailies }`
  - `refetchPelada` and `refetchDailies` exposed individually for targeted refreshes (used in remove-player, toggle-admin, add-player, edit-pelada, create-session handlers)
- Updated `PeladaDetailPage.tsx`:
  - Removed 4 useState pairs (pelada+loading, dailies+dailiesLoading, ranking+rankingLoading, awards+awardsLoading)
  - Removed 4 useCallback + 4 useEffect fetch definitions
  - Removed imports: useCallback, useEffect, getPelada, getRanking, getPeladaAwards, getDailiesForPelada, PeladaDetail, DailyListItem, RankingDTO, PeladaAwards
  - Replaced with single `usePeladaDetail(id)` call
  - `rankingSort` state and `sortedRanking` remain on the page
  - All 6 call sites using `fetchPelada`/`fetchDailies` replaced with `refetchPelada`/`refetchDailies`
- Files changed: `frontend/src/components/pelada/hooks/usePeladaDetail.ts` (new), `frontend/src/pages/PeladaDetailPage.tsx`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`
- **Learnings for future iterations:**
  - `npx tsc --noEmit` is the typecheck command for this frontend (no separate lint script needed for type correctness)
  - The `hooks/` directory under a component folder does not pre-exist — create it with mkdir before writing hook files
  - When a page uses `fetchX` in both useEffect deps and event handlers, the hook must expose named individual refetch functions, not just a combined `refetch`
  - Browser testing not available in this environment — manual verification needed for UI changes
---
